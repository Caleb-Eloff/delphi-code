unit Admin_u;

interface

uses
  Winapi.Windows, Winapi.Messages, System.SysUtils, System.Variants,
  System.Classes, Vcl.Graphics, Math,
  Vcl.Controls, Vcl.Forms, Vcl.Dialogs, Data.DB, Vcl.Grids, Vcl.DBGrids,
  Vcl.ExtCtrls, Vcl.StdCtrls, dmAccounts_u, Vcl.ComCtrls;

type
  TfrmAdmin = class(TForm)
    imgAdminBackground: TImage; // Background image for visual polish
    dbgAccounts: TDBGrid; // Grid to display user accounts
    dbgFlights: TDBGrid; // Grid to display flight bookings
    btnAdminLogOut: TButton; // Button to log out of admin panel
    lblPricing: TLabel; // Label for pricing section
    btnUpdatePrice: TButton; // Button to update price per pixel
    redPricing: TRichEdit; // RichEdit to display current pricing
    btnAccountsShowAll: TButton;
    // Button to clear filters and show all accounts
    procedure FormCreate(Sender: TObject); // Runs when form is created
    procedure FormShow(Sender: TObject); // Runs when form becomes visible
    procedure btnAdminLogOutClick(Sender: TObject);
    // Handles logout button click
    procedure FormClose(Sender: TObject; var Action: TCloseAction);
    // Handles form close
    procedure btnAccountRemoveClick(Sender: TObject); // Handles account removal
    procedure btnAccountUpdateClick(Sender: TObject); // Handles account update
    procedure btnUpdatePriceClick(Sender: TObject); // Handles price update
    procedure btnAccountCreateClick(Sender: TObject);
    // Handles account creation
    procedure btnDetailsRemoveClick(Sender: TObject); // Handles flight deletion
    procedure dbgAccountsCellClick(Column: TColumn);
    // Handles account grid cell click
    procedure btnAccountsShowAllClick(Sender: TObject);
    // Handles "Show All" button click
  private
    procedure ResizeGrids; // Resizes both grids evenly
  public
  end;

var
  frmAdmin: TfrmAdmin;

implementation

{$R *.dfm}

uses LoginRegister_u, BookFlights_u, AccountManager_u;

procedure TfrmAdmin.ResizeGrids;
begin

  // Resize columns in both account and flight grids
  TAccountManager.ResizeGridColumns(dbgAccounts);
  TAccountManager.ResizeGridColumns(dbgFlights);

end;

procedure TfrmAdmin.btnAccountCreateClick(Sender: TObject);
begin

  // Create a new account using AccountManager
  TAccountManager.CreateAccount;
  ResizeGrids;

end;

procedure TfrmAdmin.btnAccountUpdateClick(Sender: TObject);
begin

  // Update selected account using AccountManager
  TAccountManager.UpdateAccount;
  ResizeGrids;

end;

procedure TfrmAdmin.btnAccountRemoveClick(Sender: TObject);
begin

  // Remove selected account and related flights
  TAccountManager.RemoveAccount;
  ResizeGrids;

end;

procedure TfrmAdmin.btnDetailsRemoveClick(Sender: TObject);
begin

  // Check if there are any flights to delete
  if dmAccounts.tblFlights.IsEmpty then
  begin
    ShowMessage('No flights in the database.');
    Exit;
  end;

  // Confirm deletion of selected flight
  if MessageDlg('Are you sure you want to delete this flight?', mtConfirmation,
    [mbYes, mbNo], 0) <> mrYes then
    Exit;

  // Delete the selected flight
  dmAccounts.tblFlights.Delete;
  ShowMessage('Flight successfully removed.');

  // Refresh tables and resize grids
  TAccountManager.RefreshAllTables;
  ResizeGrids;

end;

procedure TfrmAdmin.btnAccountsShowAllClick(Sender: TObject);
begin

  // Clear flight filters and refresh all tables
  TAccountManager.ClearFlightFilter;
  TAccountManager.RefreshAllTables;
  ResizeGrids;

end;

procedure TfrmAdmin.dbgAccountsCellClick(Column: TColumn);
var
  sUserID: string;
begin

  // Get selected UserID from account grid
  sUserID := dmAccounts.tblAccounts.FieldByName('UserID').AsString;

  // Filter flights to show only those for selected user
  TAccountManager.FilterFlightsByUserID(sUserID);
  ResizeGrids;

end;

procedure TfrmAdmin.btnAdminLogOutClick(Sender: TObject);
begin

  // Hide admin form and return to login/register screen
  frmAdmin.Hide;
  frmLoginRegister.Show;

end;

procedure TfrmAdmin.btnUpdatePriceClick(Sender: TObject);
begin

  // Prompt admin to enter new price per pixel
  frmBookFlights.rPricePerPixel :=
    StrToFloat(InputBox('Pricing', 'Price per pixel distance:', ''));

  // Clear and format RichEdit for pricing display
  redPricing.Clear;
  with redPricing.Paragraph do
  begin

    TabCount := 1;
    Tab[0] := 50;

  end;

  // Display styled pricing info
  redPricing.Lines.Add('─────────────────────────────────────────────────');
  redPricing.Lines.Add('');
  redPricing.SelAttributes.Style := [fsBold];
  redPricing.Lines.Add('Current Rate');
  redPricing.SelAttributes.Style := [];
  redPricing.Lines.Add('');
  redPricing.Lines.Add('─────────────────────────────────────────────────');
  redPricing.Lines.Add('');
  redPricing.Lines.Add('Price per pixel:' + #9 + 'R' + FormatFloat('0.00',
    frmBookFlights.rPricePerPixel));
  redPricing.Lines.Add('');
  redPricing.Lines.Add('─────────────────────────────────────────────────');
  redPricing.Lines.Add('');

end;

procedure TfrmAdmin.FormCreate(Sender: TObject);
begin

  // Link grids to their respective data sources
  dbgAccounts.DataSource := dmAccounts.dscAccounts;
  dbgFlights.DataSource := dmAccounts.dscFlights;

  // Remove window borders and maximize form
  BorderStyle := bsNone;
  SetBounds(0, 0, Screen.Width, Screen.Height);
  WindowState := wsMaximized;

  // Load background image
  imgAdminBackground.Picture.LoadFromFile('Background.png');

  // Resize grid columns on startup
  ResizeGrids;

end;

procedure TfrmAdmin.FormShow(Sender: TObject);
begin

  // Refresh all tables when form is shown
  TAccountManager.RefreshAllTables;

  // Clear and format pricing RichEdit
  redPricing.Clear;
  with redPricing.Paragraph do
  begin
    TabCount := 1;
    Tab[0] := 50;
  end;

  // Display styled pricing info
  redPricing.Lines.Add('─────────────────────────────────────────────────');
  redPricing.Lines.Add('');
  redPricing.SelAttributes.Style := [fsBold];
  redPricing.Lines.Add('Current Rate');
  redPricing.SelAttributes.Style := [];
  redPricing.Lines.Add('');
  redPricing.Lines.Add('─────────────────────────────────────────────────');
  redPricing.Lines.Add('');
  redPricing.Lines.Add('Price per pixel:' + #9 + 'R' + FormatFloat('0.00',
    frmBookFlights.rPricePerPixel));
  redPricing.Lines.Add('');
  redPricing.Lines.Add('─────────────────────────────────────────────────');
  redPricing.Lines.Add('');

  // Resize grids after loading data
  ResizeGrids

end;

procedure TfrmAdmin.FormClose(Sender: TObject; var Action: TCloseAction);
begin

  // Terminate application when admin form is closed
  Application.Terminate;

end;

end.
